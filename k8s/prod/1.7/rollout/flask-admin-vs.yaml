apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: flask-admin-external-vs
  namespace: ms-prod
spec:
  hosts:
  - "prod-flask.kkkk.com"
  gateways:
  - flask-admin-gateway
  http:
  - match:
    - uri:
        prefix: "/"
    route:
    - destination:
        host: flask-admin
        subset: prod
      weight: 100
    - destination:
        host: flask-admin
        subset: canary
      weight: 0
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: flask-admin-internal-vs
spec:
  hosts:
  - flask-admin
  http:
  - route:
    - destination:
        host: flask-admin
        subset: prod
      weight: 100
    - destination:
        host: flask-admin
        subset: canary
      weight: 0
